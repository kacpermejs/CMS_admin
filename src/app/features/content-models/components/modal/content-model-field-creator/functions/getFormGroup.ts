import { AbstractControl, FormGroup } from "@angular/forms";

// export function getFormGroup(control: AbstractControl | null, path: string): FormGroup {
//   const found = control?.get(path);

//   if (!(found instanceof FormGroup)) {
//     throw new Error(`Expected FormGroup at path "${path}"`);
//   }
//   return found;
// }
